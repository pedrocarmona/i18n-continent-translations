RSpec.describe I18n::Continent::Translations do
  it "has a version number" do
    expect(I18n::Continent::Translations::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
